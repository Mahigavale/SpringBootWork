package jdbc;

import java.text.SimpleDateFormat;
import java.util.Date;
import java.util.Hashtable;
import java.util.Scanner;

public class stringlength {
	public static void main(String args[]) {

		
		/*
		 * Date date=new Date(); SimpleDateFormat sdf; sdf=new
		 * SimpleDateFormat("hh:mm:ss"); System.out.println(sdf.format(date));
		 * 
		 * 
		 * System.out.println("-----------------------------------------"); Double i=
		 * new Double(257.5); Double x=i.MIN_VALUE; System.out.println(x);
		 * 
		 * String s1="hello"; String s2=s1.replace('l','w'); System.out.println(s2);
		 */
			/*
			 * String[][]argument=new String[2][2]; int x; argument[0]=args;
			 * x=argument[0].length; for(int y=0;y<x;y++) {
			 * System.out.println(" "+argument[0][y]); }
			 */
	
		
			/*
			 * Double i= new Double(257.578123456789); float x1=i.floatValue();
			 * System.out.println(x1); Double x=3.14; int y=(int)Math.ceil(x);
			 * System.out.println(y);
			 */
		
	/*
		boolean option=true;
		while(option==true)
		{
			Scanner sc=new Scanner(System.in);
		System.out.println("please enter the string of which you want to calculate the length:");
		String str=sc.next();
		char[] demo= new char[str.length()];
		for(int i=0;i<str.length();i++)
		{
			demo[i]=str.charAt(i);
		}
		
		int length=0;
		for (int i=0;i<demo.length;i++)
		{
			length++;
		}
		System.out.println("the length of the given string is :"+length);
		System.out.println("if you want to continue again plese write true:");
		option=sc.nextBoolean();
}
		System.out.println("the program is exited..!");
	}
*/
	}
}